#ifndef DATAGENERATOR_H
#define DATAGENERATOR_H

#include <vector>
#include <string>
#include "Point.h"
#include "Cluster.h"

using namespace std;

class DataGenerator {
public:
    // Generates random clustered data and writes to CSV

private:
};

#endif // DATAGENERATOR_H
